{
  "generated": "2026-07-25",
  "purpose": "Phase 0.5B rebuild-pilot environment inventory (read-only; no software installed, no configuration modified)",
  "local_machine": {
    "hostname": "carters-mbp-2.lan",
    "os": "macOS 26.5.2 (Darwin 25.5.0, arm64)",
    "which_vivado": "not found",
    "vivado_version": null,
    "available_vivado_installations": [],
    "vivado_2023_2_available": false,
    "note": "Vivado has no macOS release; local builds are impossible by design, not by misconfiguration."
  },
  "remote_hosts": {
    "higgs.cse.sc.edu": {
      "source": "project memory: designated heavy-compute host (ctantley@)",
      "dns": "resolves (129.252.131.16)",
      "tcp_22": "FILTERED from current network (connect timeout, 2 attempts)",
      "vivado": "UNKNOWN — could not be inspected",
      "note": "reachable only from campus network / VPN"
    },
    "meson.cse.sc.edu": {
      "source": "named in Phase 0.5B instruction; present in ~/.ssh/known_hosts (previously used)",
      "dns": "resolves (129.252.131.213)",
      "tcp_22": "FILTERED from current network (connect timeout, 2 attempts)",
      "vivado": "UNKNOWN — could not be inspected",
      "note": "reachable only from campus network / VPN"
    }
  },
  "license_availability_xc7z020": "UNKNOWN — xc7z020 (Zynq-7000) is covered by the free WebPACK tier in Vivado 2023.2, so a paid license is not expected to be required, but this could not be confirmed on the build host",
  "vivado_shell_captures": {
    "status": "NOT CAPTURED — no reachable Vivado installation from this network",
    "prepared_script": "rebuild_pilot/capture_vivado_env.tcl (run on the build host before any pilot build; captures help place_design / route_design / write_bitstream, strategy lists, and report_property of impl_1)",
    "place_design_seed_note": "NOT assumed to exist; the pilot matrix may only use options that appear verbatim in the captured help output"
  },
  "target_part": {
    "from_project_script": "xc7z020clg400-1 (deployment_pipeline/bladei.tcl: create_project -part, target_part property)",
    "from_bit_headers": "7z020clg400 (b-field of all 1,383 corpus .bit files)"
  },
  "build_assets": {
    "rtl_v4_benchmarks": "~/Desktop/Karakchi-Research/trusthub_benchmarks/ (403 benchmark dirs; per-benchmark src/TjFree and src/TjIn RTL variants)",
    "constraints": "deployment_pipeline/Constraints/ (PYNQ-Z1_AES.xdc, PYNQ-Z1_C.xdc, PYNQ-Z1_RS232.xdc, PYNQ-Z1_empty.xdc, PYNQ-Z1_vhdl.xdc)",
    "project_creation_script": "deployment_pipeline/bladei.tcl",
    "build_driver": "deployment_pipeline/run_random_build.tcl (drives synth_1/impl_1 with place/route directives)",
    "ip": "deployment_pipeline/ip/ (key_memory, out_memory, state_memory .xci)",
    "project_files_xpr": "none found locally (created on the build host by bladei.tcl)",
    "dcps": "none found locally",
    "canonical_bitstream_corpus": "~/Desktop/Karakchi-Research/trusthub_bitstreams_v4/ (1,383 files, manifest e680fc728129d5d2)"
  },
  "repository": {
    "revision": "01a082420ac8c905ca0d537a8a35d99d27d0b44b",
    "head_subject": "improve CNN harness",
    "source_tree_status": "extensive uncommitted Phase 0/0.5 work (see git status); nothing staged; a pilot build must record the exact revision + dirty-file hashes it uses"
  },
  "blocker": "Vivado 2023.2 availability CANNOT BE CONFIRMED: no local Vivado (macOS), and both candidate build hosts are unreachable from the current network (TCP/22 filtered). Per instruction, the pilot STOPS BEFORE BUILDING. Next action requires campus network/VPN access to higgs or meson, then: (1) run capture_vivado_env.tcl, (2) confirm Vivado 2023.2 — do not substitute another release, (3) proceed with the documented pilot."
}
